node_modules
dist
dist-ssr
.vite
*.local
.DS_Store
.env
.env.*
!.env.example
npm-debug.log*
yarn-error.log*

# Personal documents dropped in the project root. This repo is public, and a CV
# carries a phone number and address — keep them out of `git add .` by default.
/*.pdf
/*.docx
